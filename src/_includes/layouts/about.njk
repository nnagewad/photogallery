{% extends 'layouts/base.njk' %}

{% block content %}
  <section>
    <h1 class='font-size-2 website-title'>{{ title }}</h1>
    <div class="details">
      {{ content | safe }}
    </div>
  </section>
{% endblock %}