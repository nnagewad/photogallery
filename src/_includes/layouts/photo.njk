{% extends 'layouts/base.njk' %}

{% block content %}
  <article id='{{ photo.title | slugify }}' class='photo-details' data-pagefind-body>
    <h1 class='font-size-2 website-page--title grid-container' data-pagefind-meta="title">{{ photo.title | updateApostrophe }}</h1>
    <figure class="flex-container--wrap">
      <img 
        src="{{ photo.image }}"
        alt="{{ photo.alt }}"
        class="
          {% if photo.dimensions.width < photo.dimensions.height %}
            photo photo-orientation--portrait
          {% elif photo.dimensions.width == photo.dimensions.height %}
            photo photo-orientation--square
          {% elif photo.dimensions.width > photo.dimensions.height %}
            photo photo-orientation--landscape
          {% endif %}
        "
        data-pagefind-meta="image_alt[alt]"
      />
      <figcaption class="photograph-details">
        <dl>
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>Date:</dt>
            <dd class='data-value'>
              <time datetime="{{ photo.dateTaken | apiToISO }}">
                {{ photo.dateTaken | apiToDate | upper }}
              </time>
            </dd>
          </div>
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>Time:</dt>
            <dd class='data-value'>
              <time datetime="{{ photo.dateTaken | apiToISO }}">
                {{ photo.dateTaken | apiToTime | upper }}
              </time>
            </dd>
          </div>
          {% if photo.gps != null %}
            <div class='list-horizontal horizontal-gap--2'>
              <dt class='data-key'>Location:</dt>
              <dd class='data-value'>
                <span data-pagefind-filter="location">{% if photo.localized != null %}{{ photo.localized | upper }}, {% endif %}{{ photo.country | upper }}</span> <a href="https://www.google.com/maps?q={{ photo.gps.lat }},{{ photo.gps.lon }}" rel="noopener noreferrer" target="_blank" class="view-map-link">(view in Google Maps)</a>
              </dd>
            </div>
          {% endif %}
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>Camera:</dt>
            <dd class='data-value' data-pagefind-filter="camera">{{ photo.camera }}</dd>
          </div>
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>Lens:</dt>
            <dd class='data-value' data-pagefind-filter="lens">{{ photo.lens }}</dd>
          </div>
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>Aperture:</dt>
            <dd class='data-value' data-pagefind-filter="aperture">{{ photo.aperture }}</dd>
          </div>
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>ISO:</dt>
            <dd class='data-value' data-pagefind-filter="iso">{{ photo.iso }}</dd>
          </div>
          <div class='list-horizontal horizontal-gap--2'>
            <dt class='data-key'>Shutter speed:</dt>
            <dd class='data-value' data-pagefind-filter="shutter-speed">{{ photo.shutterSpeed }}</dd>
          </div>
          <div class='list-horizontal horizontal-gap--2'>
          <dt class='data-key'>Tags:</dt>
          <dd>
            <ul class='list-horizontal horizontal-gap--4'>
              {% for tag in photo.tags %}
                <li class='data-value' data-pagefind-filter="tag">{{ tag | upper }}</li>
              {% endfor %}
            </ul>
          </dd>
        </div>
        </dl>
      </figcaption>
    </figure>
  </article>
  {% include 'partials/pagination.njk' %}
{% endblock %}